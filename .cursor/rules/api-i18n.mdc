---
description: 翻译与国际化约定。
alwaysApply: false
globs: tasks/**/*.py
---

# 翻译

> ⚠️ 源文件：`CLAUDE.md`。修改全局规则请同步更新 `CLAUDE.md`。

- 读接口：挂 `@translate_all_output` 装饰器，**禁**再手动 `translate_text`
- 写接口：POST 返回体 `msg` 按请求语言翻译

## 装饰器参数

| 参数 | 含义 |
|------|------|
| `modules` | 翻译来源模块名列表 |
| `translatable_fields` | 直接翻译字段 |
| `kv_fields` / `v_fields` / `k_fields` | 字典字段：翻 key+value / 仅 value / 仅 key |
| `skip_fields` | 跳过；至少 `data, pageSize, page, total, page_total, date` |
| `default_lang` | 原生语言，默认 `cn` |

## 装饰链

`urls` 路由绑定（外）→ `@translate_all_output`（中）→ `async def`（内核）

## POST msg 翻译（写接口必做）

```python
from apps.system.<任务包名>.translate import translate_text

is_trans, translation_dict = get_translaiton_dict_from_request(
    request, ["<任务包名>", "common", "common_filters", "msg"]
)
msg = translate_text("操作成功", is_trans, translation_dict)
return {"code": 200, "msg": msg, "data": data}
```
